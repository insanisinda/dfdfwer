apiVersion: v1
kind: Service
metadata:
    name: "rtrtrt-1424"
    labels:
        app: "rtrtrt-1424"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "rtrtrt-1424"